export default function LearningProgressSummary({ label = 'Learning progress', completed = 0, total = 0, detail, className = '' }) {
  const percentage = total > 0 ? Math.round((completed / total) * 100) : 0;
  return <section className={`rounded-3xl bg-surface-raised p-6 shadow-[0_20px_48px_-38px_rgba(20,20,18,0.4)] ${className}`} aria-label={label}><div className="flex items-end justify-between gap-4"><div><p className="text-[11px] font-bold uppercase tracking-[0.13em] text-text-dim">{label}</p><p className="mt-2 font-display text-3xl font-extrabold text-text-primary">{completed} of {total}</p>{detail && <p className="mt-1 text-sm font-medium text-text-muted">{detail}</p>}</div><span className="font-mono text-sm font-bold text-accent">{percentage}%</span></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-surface-soft" role="progressbar" aria-label={`${label}: ${percentage}%`} aria-valuemin="0" aria-valuemax="100" aria-valuenow={percentage}><div className="h-full rounded-full bg-accent transition-[width]" style={{ width: `${percentage}%` }} /></div></section>;
}
